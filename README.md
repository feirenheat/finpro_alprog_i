# Cara Compile di Windows:

## Server:

g++ server.cpp -o server -lws2_32

## Client:

g++ client.cpp -o client -lws2_32

# Run Program:

1. Gunakan dua terminal (untuk client dan untuk server)
2. Masukkan `.\server.exe` pada terminal pertama.
3. Masukkan `.\client.exe` pada terminal kedua.
